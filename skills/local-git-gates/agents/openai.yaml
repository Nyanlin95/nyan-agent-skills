interface:
  display_name: "Local Git Gates"
  short_description: "Manage target-repository local Git gates"
  default_prompt: "Use $local-git-gates to design, install, review, route, and verify local Git hooks and checks for the target repository."
