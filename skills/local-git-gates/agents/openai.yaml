interface:
  display_name: "Local Git Gates"
  short_description: "Design target-repository Git verification gates"
  default_prompt: "Use $local-git-gates to add local Git checks derived from this repository's existing verification commands."
