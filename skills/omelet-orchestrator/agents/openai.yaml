interface:
  display_name: "Omelet Orchestrator"
  short_description: "Operate or configure the repository's primary Omelet orchestration"
  default_prompt: "Use $omelet-orchestrator to operate or configure the primary Omelet orchestrator for this repository. Infer the work posture and intended artifact state from the user's action, subject, deliverable, quantity, and constraints. Omelet may work directly; delegate only when a specialist or independent result materially improves the outcome. Plan in semantic orchestration patterns and reconstruct the task semantic from discovered work. For visible product design, establish intent and protected behavior, bound what references govern, and let the design specialist choose the visual means."
