interface:
  display_name: "OpenCode CLI"
  short_description: "Delegate bounded implementation to OpenCode"
  default_prompt: "Use $opencode-cli to delegate this limited, non-sensitive implementation task within explicit file or folder paths."
